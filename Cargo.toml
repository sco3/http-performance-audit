[package]
name = "http-performance-audit"
version = "0.1.0"
edition = "2024"

[dependencies]
ntex = { version = "3.1.0", features = ["tokio"] }

[profile.release]
lto = "fat"
codegen-units = 1
opt-level = 3
panic = "abort"
